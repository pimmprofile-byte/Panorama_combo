# 비밀 키 — 절대 커밋 금지
.env

# 파이썬
__pycache__/
*.py[cod]
.venv/
venv/
env/

# OS
.DS_Store
Thumbs.db

# 화면에서 고친 조사카드 덧쓰기 — 서버가 돌면서 쌓는 «상태»다.
# 원고가 아니므로 판에 안 싣는다(싣는 순간 배포가 남의 고친 것을 덮어쓴다).
# 고친 것을 원고로 옮길 때는 관리자의 「고친 원고 내보내기」로 꺼내 간다.
scenario_edits/
